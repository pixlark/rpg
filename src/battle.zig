const std = @import("std");
const engine = @import("engine.zig");
const ui = @import("ui.zig");
const global = @import("global.zig");

pub fn run(context: *engine.Context) !void {
    // TODO(pixlark): Implement battles
    return error.Unimplemented;
}
